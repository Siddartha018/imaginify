import { SignUp } from '@clerk/nextjs'
import React from 'react'

/**
 * Renders the SignUp page component
 * @returns {JSX.Element} A React component that renders the SignUp page
 */
const SignUpPage = () => {
  return <SignUp/>
}

export default SignUpPage