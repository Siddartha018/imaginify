import { SignIn } from '@clerk/nextjs'
import React from 'react'

/**
 * Renders the sign-in page component.
 * @returns {JSX.Element} A React component that renders the SignIn component.
 */
const SignInPage = () => {
  return <SignIn/>
}

export default SignInPage